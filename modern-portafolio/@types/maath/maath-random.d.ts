declare module 'maath/random/dist/maath-random.esm' {
    export function inSphere(buffer: Float32Array, options: { radius: number }): Float32Array;
    // Agrega aquí cualquier otro tipo o función que necesites exportar
  }